export interface UserData {
    name: string
    username: string
    email: string
    bio: string
    location: string
    website: string
    avatar: string
    cover_photo: string
    date_of_birth: string
}

export interface EditBody {
    name: string
    username: string
    bio: string
    location: string
    website: string
    avatar: string
    cover_photo: string
    date_of_birth: string
}